pipeline {
    agent any


    stages {
        // THis is build step
        /*
            Everything will explained.
         */

         stage('Build Docker image') {
            steps {
                sh 'docker build -f ci/Dockerfile -t my-playwright .'
            }
         }
    }
}